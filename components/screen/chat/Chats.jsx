import { useEffect, useState } from "react"
import { Text } from "react-native"

export const Chats = () => {
    const [chats, setChats] = useState(null);
    useEffect(() => {
        
    }, [])
    return (
        <Text>Chat List</Text>
    )
}